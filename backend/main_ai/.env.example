# SQLite database path. The application creates the directory and database automatically.
DATABASE_PATH=data/conversations.sqlite3

# Main AI provider settings
OPENROUTER_API_KEY=your-openrouter-key
OPENROUTER_MODEL=openai/gpt-4o-mini

# Local file automation sandbox
FILE_AUTOMATION_BASE_DIR=C:/path/to/automation-workspace
